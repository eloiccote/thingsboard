#
# Copyright © 2016-2026 The Thingsboard Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name:  ThingsBoard CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: thingsboard
          POSTGRES_USER: thingsboard
          POSTGRES_PASSWORD: thingsboard
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: "maven"
      
      - name: Compile and Install Dependencies
        run: >
          mvn clean install 
          -DskipTests=true 
          -DskipITs=true 
          -Dcheckstyle.skip=true 
          -Dlicense.skip=true 
          -Ddockerfile.skip=true 
          -B
      
      - name: Run Unit Test with Maven
        run: >
          mvn test 
          -pl common/data 
          -am 
          -Dtest=!**/*IT 
          -Dtest=!**/*IntegrationTest 
          -DskipITs=true 
          -Dcheckstyle.skip=true 
          -Dlicense.skip=true 
          -Ddockerfile.skip=true 
          -B

      - name: Database Setup
        run: |
          chmod +x application/target/bin/install/install_dev_db.sh
          cd application/target/bin/install
          ./install_dev_db.sh
      
      - name: Starting and Verify ThingsBoard is running
        run: |
          echo "Démarrage de ThingsBoard..."
          java -jar application/target/thingsboard-*.jar &
          PID=$!

          echo "Attente de la disponibilité (Health Check)..."
          for i in {1..30}; do
            sleep 10
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/login)
            if [ "$STATUS" == "200" ]; then
              echo "SUCCÈS : ThingsBoard est en ligne !"
              kill $PID
              exit 0
            fi
            echo "Tentative $i/30 : Statut $STATUS - En attente..."
          done
          
          echo "ÉCHEC : Le serveur n'a pas démarré à temps."
          cat logs/thingsboard.log || echo "Pas de log trouvé"
          kill $PID
          exit 1